{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/user-service.csproj"
            ],
            "problemMatcher": {
                "base": "$msCompile",
                "fileLocation": [
                    "relative",
                    "${workspaceRoot}"
                ]
            }
        }
    ]
}